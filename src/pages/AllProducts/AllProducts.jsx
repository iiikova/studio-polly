import React from "react";
import s from "./AllProducts.module.scss";

function AllProducts() {
  return (
    <div className={s.outerWrap}>
      <div className={"container"}>
        <div className={s.inner}>
          <h1>AllProducts</h1>
          <p>
            Whenever the location changes, looks through all its child routes to
            find the best match and renders that branch of the UI. elements may
            be nested to indicate nested UI, which also correspond to nested URL
            paths. Parent routes render their child routes by rendering
            anWhenever the location changes, looks through all its child routes
            to find the best match and renders that branch of the UI. elements
            may be nested to indicate nested UI, which also correspond to nested
            URL paths. Parent routes render their child routes by rendering
            anWhenever the location changes, looks through all its child routes
            to find the best match and renders that branch of the UI. elements
            may be nested to indicate nested UI, which also correspond to nested
            URL paths. Parent routes render their child routes by rendering
          </p>
        </div>
      </div>
    </div>
  );
}

export default AllProducts;
